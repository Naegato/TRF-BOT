services:
  app:
    image: node:24
    restart: unless-stopped
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DB_PATH: ${DB_PATH}
    volumes:
      - .:/app
      - sqlite_data:/data

volumes:
  sqlite_data:
